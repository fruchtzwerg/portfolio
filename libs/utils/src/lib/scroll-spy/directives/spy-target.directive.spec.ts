import { SpyTargetDirective } from './spy-target.directive';

describe('SpyTargetDirective', () => {
  it('should create an instance', () => {
    const directive = new SpyTargetDirective();
    expect(directive).toBeTruthy();
  });
});
